{
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "https://node-api-puce.vercel.app/api/(.*)"},
    { "src": "/twitter/(.*)", "dest": "/twitter/(.*)"},
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}